package com.xuanfeng;

public class XuanFengDaoGenerator {
    /*public static void main(String[] arg) throws Exception {
        // 正如你所见的，你创建了一个用于添加实体（Entity）的模式（Schema）对象。
        // 两个参数分别代表：数据库版本号与自动生成代码的包路径。
        Schema schema = new Schema(1, "com.xuanfeng.xuanfengweather.greendao");
        createAppKv(schema);
        new DaoGenerator().generateAll(schema, "D:/XuanFengWeather/app/src/main/java-gen");
    }

    //创建KV表
    private static void createAppKv(Schema schema) {
        Entity AppKv = schema.addEntity("AppKv");
        AppKv.addIdProperty().primaryKey();
        AppKv.addStringProperty("appKey").notNull();
        AppKv.addStringProperty("appVal");
    }*/
}
